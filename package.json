{
    "name": "portfolio-mono-repo",
    "description": "Project Outter package.json - Will include scripts for Heroku deployment",
    "engines": {
      "node": "16.x"
    },
    "scripts": {
      "start": "npm start --prefix client",
      "build": "npm install --prefix client && npm run build --prefix client",
      "backend": "rails db:create && rails db:migrate && rails db:seed && bundle install",
      "clean": "rm -rf public",
      "deploy": "cp -a client/build/. public/",
      "render": "npm run clean && npm run backend && npm run build && npm run deploy"
    }
  }